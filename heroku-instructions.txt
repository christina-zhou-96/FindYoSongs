Maintenance instructions:

After local changes:
1. Update requirements file
`pip freeze > requirements.txt`

2. Login to Heroku through cmd in the root project directory

3. Push to heroku
`git push heroku master`

